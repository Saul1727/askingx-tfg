Actúa como un Frontend Senior en React. El modal visual de CreateAskModal.jsx ya funciona perfectamente, pero ahora necesitamos conectarlo al backend real y mejorar la UX del formulario.

Requisito 1: Crea el servicio API (src/services/askService.js)

Exporta una función asíncrona createAsk(askData).

Recupera el JWT del localStorage (localStorage.getItem('token')).

Haz un fetch POST a http://localhost:3000/api/asks.

En los headers, incluye 'Content-Type': 'application/json' y 'Authorization': \Bearer ${token}``.

El body será JSON.stringify(askData).

Maneja los errores: si !response.ok, lanza un error con el mensaje del backend. Si todo va bien, devuelve la respuesta parseada.

Requisito 2: Actualiza src/components/asks/CreateAskModal.jsx

Importa la función createAsk del servicio.

Añade dos nuevos estados: isLoading (boolean) y error (string).

Modifica la función handleSubmit:

Pon isLoading a true y limpia errores previos.

Llama a createAsk(formData).

Si tiene éxito: muestra un alert('¡Petición creada con éxito!'), resetea el formData al estado inicial, y cierra el modal (onClose()).

Si falla (bloque catch): captura el error y guárdalo en el estado error.

En el finally, pon isLoading a false.

Mejoras UI (UX):

Si hay un error, muéstralo en un cuadro rojo (bg-red-100 text-red-700 p-3 rounded mb-4) justo encima de los campos.

Mientras isLoading sea true, desactiva el botón de "Registrar Petición" y cambia su texto a "Guardando...".

Devuélveme el código completo de askService.js y el código actualizado de CreateAskModal.jsx.