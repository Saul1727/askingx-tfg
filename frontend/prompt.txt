Actúa como Arquitecto de Software Frontend y experto en React + Tailwind CSS v3.
Tu tarea es construir el esqueleto principal de la aplicación (Layout) y el panel de control (Dashboard) basándote exactamente en la imagen adjunta.

Vamos a dividir el trabajo en componentes limpios y escalables. Usaremos react-router-dom.

Requisito 1: Arquitectura de Componentes (Clean Code)
Genera el código para los siguientes archivos respetando el Principio de Responsabilidad Única:

src/components/layout/Sidebar.jsx: La barra lateral oscura.

src/components/layout/Topbar.jsx: La barra superior con buscador y perfil.

src/components/layout/MainLayout.jsx: El contenedor global que junta Sidebar, Topbar y un <Outlet /> para inyectar las páginas.

src/pages/Dashboard.jsx: El contenido central de la imagen (tarjetas de estadísticas y tabla de peticiones).

Requisito 2: Lógica de Rol en el Sidebar (Sidebar.jsx)

Simula la lectura del rol del usuario (asume que obtenemos role = 'AUTHOR' de un hook o contexto ficticio por ahora).

Renderiza los enlaces condicionalmente:

Organizaciones (Askers) y Peticiones (Asks) deben verse si es AUTHOR o ADMIN.

La sección Admin -> Usuarios solo debe verse si el rol es ADMIN.

Deja los iconos preparados (usa lucide-react).

Requisito 3: Diseño de la Vista Central (Dashboard.jsx)

Recrea fielmente el encabezado ("Panel - AskAuthor").

Crea las tres tarjetas superiores de estadísticas (Total Organizaciones, Peticiones Abiertas, Peticiones Completadas).

Recrea la tabla inferior con las pestañas "Mis Organizaciones" y "Mis Peticiones".

La tabla debe tener las columnas exactas de la imagen: Título, Organización, Tipo, Dominio, Vencimiento, Estado, Acciones.

Botón Nueva Petición: El botón azul "+ Nueva Petición" debe estar bien posicionado, por ahora solo le pondremos un onClick={() => console.log('Abrir modal CU-01')}.

Los "Estados" en la tabla (ABIERTA, COMPLETADA) deben tener ese pequeño círculo de color asociado (ej: amarillo para abierta, verde para completada).

Devuélveme el código completo de estos 4 componentes, listos para integrarlos en nuestro sistema de rutas. Asegúrate de que el diseño sea responsive.